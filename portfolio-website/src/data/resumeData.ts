import rawData from '../../../homepage/portfolio/master-resume.json';
import type { ResumeData } from '../types/resume.types';

// Export the typed resume data
export const resumeData = rawData as ResumeData;

export default resumeData;
